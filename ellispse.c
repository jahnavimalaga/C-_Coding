#include<stdio.h>
#include<graphics.h>
int main()
{
	int a,b;
	int gd=DETECT,gm;
	printf"enter two no a,b:"
    scanf("%d%d",&a,&b);
    initgraph(&gd,&gm,NULL);
    while(x<=y)
    {
    	x=x+1
    	putpixel(x,y,RED);
    	
	}
 delay(50);
 getch();
 closegraph();
 return 0;

}

